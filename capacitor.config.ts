import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.MantiCreative.scripture',
  appName: 'scripture-reader',
  webDir: 'dist'
};

export default config;
